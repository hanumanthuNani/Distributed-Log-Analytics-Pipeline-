kafka {
  bootstrap-servers = "localhost:9092"
  topic = "logs"
}

spark {
  app-name = "DistributedLogAnalytics"
  checkpoint-location = "/tmp/spark-checkpoints/logs"
}

hbase {
  table = "log_analytics:log_levels"
}

hive {
  database = "analytics"
  table = "log_level_counts"
}
